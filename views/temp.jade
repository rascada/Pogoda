#tempSect.sect.blockShadow
	h1 Temperatura
	p Dziś temperatura wynosi: 
		span#temperatureAvg
		°C
	//#cos
		span#temperatureCurrent
		span#temperatureReal 
		span#temperatureDewPoint
		span#temperatureTrend

	.thermometer
		.measure
			-for(var i=30;i--;)
				-if(i % 5 == 0 && i != 30)
					div.line.boldLine
						p #{i}
				-else
					.line
		.sensor
			#temperatureCurrentSensor.sensorVal
		.bottom
